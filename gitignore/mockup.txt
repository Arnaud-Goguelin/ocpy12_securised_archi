## app architecture:

=> Layered Architecture : cf. https://codewithandrea.com/articles/flutter-repository-pattern/

epic_events/
├── models/ # SQLAlchemy model (pure schema)
│   ├── user.py
│   ├── customer.py
│   ├── contract.py
│   └── event.py
├── repositories/ # get_by_id(), get_all(), create(), all the logic to make the queries to DB
│   ├── user_repository.py
│   ├── customer_repository.py
│   └── ...
├── services/ # Business rules + permission checks
│   ├── users
│   │   ├── permissions.py
│   │   ├── service.py
│   ├── customers
│   └── ...
├── controllers/ parse CLI input → call service → call view
│   ├── user_controller.py
│   └── ...
├── views/  Format + print tables/messages
│   ├── user_view.py
│   └── ...
├── utils/
│   ├── config.py
│   └── constants.py
└── main.py

ToDo: could be good to use Pydantic to validate inputs between controllers and services, could also be used to return
 clean response from an object in DB and could allow to validate multiple fields at once (e.g. in contract:
 total_amount and remaining_amount are linked and remaining_amount must not be > to total_amount)
